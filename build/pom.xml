<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
    <parent>
        <artifactId>extensions</artifactId>
        <groupId>org.jboss.teiid</groupId>
        <version>1.3.0</version>
    </parent>
    <groupId>org.jboss.teiid.extensions</groupId>
    <modelVersion>4.0.0</modelVersion>
    <artifactId>build-extensions</artifactId>
    <name>Build</name>
    <description>Teiid Extensions Build</description>
    
    <dependencies>
		<dependency>
			<groupId>org.jboss.teiid.extensions</groupId>
			<artifactId>database-service</artifactId>
			<type>ejb</type>
		</dependency>
		<dependency>
			<groupId>org.jboss.teiid.extensions</groupId>
			<artifactId>database-logging-appender</artifactId>
		</dependency>  
    </dependencies>
    <build>
        <outputDirectory>target/kits</outputDirectory>
        <resources>
            <resource>
                <directory>kits</directory>
                <filtering>true</filtering>
                <includes>
                    <include>**/*.xml</include>
                    <include>**/*.properties</include>
                    <include>**/*.sh</include>
                    <include>**/*.bat</include>
                    <include>**/*.html</include>
                    <include>**/*.cli</include>
                </includes>
            </resource>
            <resource>
                <directory>kits</directory>
                <filtering>false</filtering>
                <excludes>
                    <exclude>**/*.xml</exclude>
                    <exclude>**/*.properties</exclude>
                    <exclude>**/*.sh</exclude>
                    <exclude>**/*.bat</exclude>
                    <exclude>**/*.html</exclude>
                    <exclude>**/*.cli</exclude>
                </excludes>
            </resource>
        </resources>
        <pluginManagement>
            <plugins>
                <plugin>
                    <artifactId>maven-assembly-plugin</artifactId>
                    <configuration>
                        <archiverConfig>
                            <fileMode>0644</fileMode>
                            <directoryMode>0755</directoryMode>
                            <defaultDirectoryMode>0755</defaultDirectoryMode>
                        </archiverConfig>
                    </configuration>
                </plugin>
            </plugins>
        </pluginManagement>
        <plugins>
            <plugin>
                <artifactId>maven-assembly-plugin</artifactId>

                <configuration>
                    <descriptors>
                        <descriptor>jboss-as7-extensions-dist.xml</descriptor>
                    </descriptors>
                </configuration>

                <executions>
                    <execution>
                        <id>create-assemblies</id>
                        <phase>package</phase>
                        <goals>
                            <goal>single</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>
